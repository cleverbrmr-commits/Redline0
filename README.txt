REDLINE unified bot package

Included in this build
- /clients private panel with improved embeds and category-first flow
- /clientpanel send public panel message that still returns private downloads/results
- /upload, /editclient, /removeclient
- role-based visibility, categories, cooldowns, logging, announcements, export/backup
- moderation features including prison system from the upgraded build

Notes
- Keep your real .env values set in Railway or local environment.
- Local files are still stored in uploads/ and metadata in modules.json.
- This index.js passed syntax check with: node --check index.js
